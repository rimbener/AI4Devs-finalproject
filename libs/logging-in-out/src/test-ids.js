/**
 * ESM testIDs for @helsoft/logging-in-out/test-ids (Vite/Storybook).
 * Playwright Node require() uses test-ids.cjs — keep both in sync.
 */

export const LOGIN_EMAIL_FIELD_TEST_ID = 'login-form-email-field';
export const LOGIN_PASSWORD_FIELD_TEST_ID = 'login-form-password-field';
export const LOGIN_SUBMIT_BUTTON_TEST_ID = 'login-form-submit-button';
