{
  "name": "@helsoft/supabase-services",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "jest --coverage=false --passWithNoTests --watchman=false",
    "test:rls": "jest --config jest.rls.config.js --coverage=false --watchman=false --runInBand",
    "test:watch": "jest --watch",
    "mutation": "stryker run"
  },
  "dependencies": {
    "@helsoft/types": "workspace:*",
    "@supabase/supabase-js": "^2.49.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@stryker-mutator/core": "^8.2.6",
    "@stryker-mutator/jest-runner": "^8.2.6",
    "@stryker-mutator/typescript-checker": "^8.2.6",
    "@types/jest": "~29.5.14",
    "@types/node": "~26.1.0",
    "jest": "~29.7.0",
    "ts-jest": "^29.4.11",
    "typescript": "~6.0.3"
  }
}
