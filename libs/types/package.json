{
  "name": "@helsoft/types",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "jest --coverage=false --passWithNoTests --watchman=false",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/jest": "~29.5.14",
    "jest": "~29.7.0",
    "ts-jest": "^29.4.11",
    "typescript": "~6.0.3"
  }
}
