# Current session

**Active feature:** ai-provider-registry-frontend
**Notes:** Sharing the existing `feat/ai-provider-registry-backend` worktree/branch (not a fresh worktree) — this frontend story's ACs assume the backend schema/edge-function contract from `ai-provider-registry-backend` (`pr_ready`, not yet merged), so it builds on top of that branch. Effective base ref for review/mutation diffs is `feat/ai-provider-registry-backend`, not `feature-entrega3-HernanLaura` directly. Docs seeded at `docs/features/ai-provider-registry-frontend/`, phase `pending`.
