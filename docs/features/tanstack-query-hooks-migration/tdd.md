# TDD log — tanstack-query-hooks-migration

## Slice 0 — auth-change cache reset (task-1, `use-session.ts`)
s1-s4: evict-other-users-on-sign-in / preserve-on-same-user-refresh / evict-on-sign-out / write-session-after-eviction.
Cycles: unconditional `removeQueries({predicate: key[0]!=='auth'})` before `setQueryData` → `previousUserIdRef` guard so only real user-id changes evict. Full suite 162 green; lint+types clean.

## Slice 1 — use-lesson (task-2)
s5-s9: load / empty-slides / error / refetch-clears-error / id-switch never shows stale id.
Cycles: `useQuery({queryKey: lessonQueryKey(id)})`, deleted `use-lesson.reducer.ts`; `createWrapper()` in tests; per-id key isolates caches, no manual guard. Dropped 2 stale-refetch-race characterization tests (no `@s`). `lesson-player.integration.test.ts` adapted. No `instanceof Error` normalizer re-added. 161 green; repo-wide 14/14 clean.

## Slice 2 — use-lessons (task-3)
s10-s16: load/empty/error/refetch-clears-error, delete removes from cache (no re-read), failed delete rejects+list unchanged, D4 delete-error-outranks-read-error+cleared-by-refetch.
Cycles: `useQuery(['lessons'])` + delete `useMutation`; deleted `use-lessons.reducer.ts`. D4: `error = deleteMutation.error ?? query.error`; `refetch` calls `deleteMutation.reset()` before `query.refetch()`. Dropped 6 isMounted/requestId/unmount characterization tests. `lessons.integration.test.ts` adapted; one flaky sync-read fixed with `waitFor`. 156 green; repo-wide 14/14 clean.

## Slice 3 — use-pdf-documents (task-4)
s17-s23: mirrors task-3 1:1 (`pdfDocumentsQueryKey`, `PdfDocumentsService`, D4). GREEN on first pass; deleted `use-pdf-documents.reducer.ts`; dropped 8 characterization tests. `pdf-documents.integration.test.ts` given local `createWrapper()`. 150 green; repo-wide 14/14 clean.

## Slice 4 — use-slide-image-url (task-5 + task-6)
s24-s29: TTL-derived staleTime/gcTime both under the TTL; no-ref→null/not-loading/service-never-called; ref→loading-then-url; signing-failure degrades without throw; stale response for a previous storagePath never wins; cache-window re-view skips a second sign.
Cycles: (1) task-5 exported `SIGNED_URL_TTL_SECONDS` from the service, no behavior change. (2) RED: migration-anchor + s24 (introspects `queryCache.find(key)?.options.staleTime/gcTime`) + s25-29. (3) GREEN: `useQuery({queryKey: slideImageQueryKey(storagePath), enabled: Boolean(storagePath), staleTime: CACHE_WINDOW_MS, gcTime: CACHE_WINDOW_MS})`, `CACHE_WINDOW_MS=(TTL-60)*1000`; v5's `isPending && isFetching` gives correct disabled-query `isLoading` for free. (4) Deleted `next-request-id.ts` + test (repo-wide grep: zero remaining importers). 152 green; repo-wide clean.

## Slice 5 — use-lesson-attempt (task-7)
s30-s36: saving→saved transitions; failed save→error/no attempt; separate save after settle is a fresh insert; two same-tick calls insert once (s33 NEW); in-flight save refuses a second; retry replays last input; retry no-ops (no prior call / already in flight).
Cycles: RED migration-anchor (save must register in `getMutationCache()`) → GREEN `useMutation({mutationFn:saveAttempt, onSettled})`, status mapped pending→saving/success→saved; kept **one** `isSaving` ref (D5) as pure entry gate (TanStack doesn't dedupe `mutate()`), cleared in `onSettled`; deleted `isMounted`+`lastInput`, retry replays `mutation.variables`. Mutation dispatch lands a tick behind scheduling — adapted sync-post-`act()` reads to `waitFor`. `lesson-results.integration.test.tsx` adapted to `{wrapper: QueryProvider}`. 154 green; repo-wide clean (1 pre-existing unrelated flake confirmed via `git stash`).

## Slice 6 — use-api-key + ApiKeyProvider removal (task-8)
s37-s50: authenticated load under `apiKeyStatusQueryKey(userId)`; unauthenticated → empty/not-loading/service-never-called; session-resolving → loading, service not called yet; user change → new key, old never seen; same-user replace → no re-read; in-flight read before logout never clobbers reset empty status; save/remove write returned status via `setQueryData`, no re-read; failed save → normalized code, status preserved, rejects; unrecognized failure → `network_error`; successful remove clears an error left by a failed save (D3); isSubmitting true during save/remove; two consumers share one read with no provider (s49, replaces deleted `ApiKeyProvider`-sharing tests); raw key never retained.
Cycles: RED 16 tests vs old reducer+Context hook, fails on missing `apiKeyStatusQueryKey`. GREEN: `useQuery({..., enabled: Boolean(sessionUserId) && !isSessionLoading})` + **one** `useMutation` over `{kind:'save'|'remove'}` (D3); `isLoading = isSessionLoading || (Boolean(sessionUserId) && isPending)`; `onSuccess` → `setQueryData`, no invalidate. Deleted `ApiKeyProvider`/context/`skip`/`useApiKeyState`, `use-api-key.reducer.ts`+test. Tick-lag fix: assertions right after an awaited `mutateAsync()` need `waitFor` (observer notifies a tick behind the settled promise). Removed `ApiKeyProvider` from `_layout.tsx` and the `app-layout-settings.test.tsx` mock. `api-key.integration.test.ts` given the optional-`queryClient` `createWrapper` signature. 18 suites/142 tests green (3 runs); repo-wide 12/12 + 14/14 clean.

## Slice 7 — use-profile + ProfileProvider removal (task-9)
s51-s58: authenticated load under `profileQueryKey(userId)`; unauthenticated → no profile/not-loading/service-never-called; loading ORs session+api-key+profile-read (3 examples); profile withheld while loading/errored; canCreate unchanged (platform plan / saved key / neither); retry re-reads a failed profile, clears error; session→unauthenticated resets profile via the user-scoped key + `enabled` gate (no manual reset); two consumers share one read with no provider (s58, replaces the deleted `ProfileProvider`-sharing test).
Cycles: RED 13 tests (migration anchor + s51-s58) vs old reducer+Context hook, fails on missing `profileQueryKey` + no cache dedupe. GREEN: `useQuery({queryKey: profileQueryKey(sessionUserId ?? ''), enabled: Boolean(sessionUserId) && !isSessionLoading})` composed with `useSession`+`useApiKey`; `isLoading = isSessionLoading || isApiKeyLoading || (Boolean(sessionUserId) && isPending)`; `retry` wraps `refetch`; `canCreate` unchanged. Deleted `ProfileProvider`/context/`skip`/`useProfileState`, `use-profile.reducer.ts`, `ProfileProviderProps`. Removed from `_layout.tsx` (`QueryProvider` alone now wraps `RootValidation`, which still calls `useProfile()` directly — verified guard behavior unchanged) and the dead `app-layout-settings.test.tsx` mock (that file never imported `@helsoft/hooks`). Retry-race fix: after an error, `isPending` is already false (status `'error'`, not `'pending'`) even mid-refetch, so the retry test asserts on settled `profile`, not `isLoading`, avoiding the slice 5/6 tick-lag class. Repo-wide consumer grep: every non-`libs/hooks` `useProfile` consumer mocks `@helsoft/hooks` at the module boundary — no hidden `QueryClientProvider` needed. `@helsoft/hooks` 18 suites/136 tests green (3 consecutive runs); lint+check-types clean. Repo-wide `pnpm turbo run test`/`check-types --force` 12/12 and 14/14 clean.
Fixup (reviewer_slice finding): RED — non-Error (Postgrest-shaped) rejection test failed `toBeInstanceOf(Error)`. GREEN — `error = queryError instanceof Error ? queryError : new Error(String(queryError))`, restoring the deleted reducer's exact guard. `@helsoft/hooks` 18/137 green, lint/check-types clean, repo-wide test 12/12 + check-types 14/14 clean.
