{
  "name": "ai-study-buddy",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "biome check --write .",
    "check-types": "turbo run check-types",
    "bootstrap": "pnpm install && pnpm check-types && pnpm lint && pnpm test",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "test:e2e:ci": "turbo run test:e2e:ci",
    "clean-modules": "rm -rf node_modules .turbo apps/*/node_modules apps/*/.expo apps/*/.turbo apps/*/dist libs/*/node_modules libs/*/.turbo libs/*/dist libs/*/storybook-static",
    "clean-watchman": "watchman watch-del-all",
    "clean": "pnpm clean-modules && pnpm clean-watchman",
    "supabase:restart": "bash supabase/restart.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@playwright/test": "1.48.0",
    "turbo": "^2.10.4"
  }
}
